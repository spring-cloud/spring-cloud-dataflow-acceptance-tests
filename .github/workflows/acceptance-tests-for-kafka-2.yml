
name: SCDF AT Kafka - env 2

# Controls when the workflow will run
on:
  schedule:
  - cron: "0 8 * * *"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
    kafka-2:
        uses: ./.github/workflows/at-common-workflow.yml
        with:
            cf_host: benicia
            cf_index: 2
            binder: kafka
