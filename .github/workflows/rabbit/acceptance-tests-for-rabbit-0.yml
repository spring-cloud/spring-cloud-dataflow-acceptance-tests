
name: SCDF AT for CF tile (Rabbit - env 0)

# Controls when the workflow will run
on:
  schedule:
  - cron: "0 4 * * *"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
    kafka-1:
        - uses: ./.github/workflows/at-common-workflow
          with:
              cf_host: alturas
              cf_index: 0
              binder: rabbit
