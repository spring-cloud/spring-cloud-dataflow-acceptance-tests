
name: SCDF AT for CF tile (Rabbit - env 1)

# Controls when the workflow will run
on:
  schedule:
  - cron: "0 8 * * *"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
    rabbit-1:
        - uses: ./.github/workflows/at-common-workflow
          with:
              cf_host: host 1
              cf_index: 1
              binder: rabbit
